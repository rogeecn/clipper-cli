const fixturePublisher = {
  name: 'fixture-publisher',
  resolveOutput() {
    return {
      entryFile: 'fixture.md',
      assetDir: 'assets'
    }
  }
}

const plugin = {
  name: 'clipper-plugin-local',
  collectors: [],
  transformers: [],
  publishers: [fixturePublisher]
}

export default plugin
export { plugin, fixturePublisher }
